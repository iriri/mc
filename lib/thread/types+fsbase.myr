use sys

pkg thread =
	type tid = sys.pid /* 32 bits on all of the fsbase platforms */
	type key = uint32

	pkglocal type tlshdr = struct
		tid   : tid
		len   : key
		base  : byte#
		stksz : sys.size
		slots : void#[...]
	;;
;;
