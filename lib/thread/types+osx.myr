use sys

pkg thread =
	type tid = sys.pid /* 64 bits */
	type key = uint64

	pkglocal type tlshdr = struct
		tid   : tid
		len   : key
		base  : byte#
		stksz : sys.size
		self  : tlshdr#
		slots : void#[...]
	;;
;;
